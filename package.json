{
  "name": "recipes-pages-build",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Post-processes the Paprika HTML export before it is published to GitHub Pages.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "test": "node --test tools/*.test.ts",
    "inject-viewport": "node tools/inject-viewport.ts",
    "verify-site": "node tools/verify-site.ts",
    "build-recipes-json": "node tools/build-recipes-json.ts"
  }
}
